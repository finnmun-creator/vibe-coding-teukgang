# 번개장터 x 바이브 코딩 특강 디자인 시스템

> **UXUI 전문가 관점**: 세련된 모노톤 기반 + 절제된 브랜드 컬러 적용

---

## 1. 브랜드 분석: 번개장터

### 브랜드 아이덴티티

| 요소 | 특징 |
|------|------|
| **핵심 컬러** | 코랄 레드 (#FF6B54) - 따뜻하고 활기찬 중고거래 플랫폼 이미지 |
| **보조 컬러** | 블랙 (#1A1A1A), 화이트 (#FFFFFF) |
| **톤 앤 매너** | 친근하고 신뢰할 수 있는, 빠르고 효율적인 |
| **시각 언어** | 깔끔한 모바일 퍼스트, 강한 CTA, 명확한 정보 계층 |
| **타겟** | 2030 중고거래 유저, 실용적이고 트렌디한 |

### 전략적 적용 방향

```
1. 모노톤 기반 설계
   - 전체 레이아웃은 흑백 스펙트럼 활용
   - 텍스트 계층: 5단계 회색 팔레트

2. 번개장터 코랄은 "번개"처럼
   - CTA 버튼 (클릭 유도)
   - 활성 상태 (현재 위치)
   - 핵심 메시지 강조 (1-2곳만)
   - 인터랙티브 요소 호버

3. 프리미엄 디테일
   - 미세한 그림자 (0.06-0.1 alpha)
   - 얇은 보더 (1px)
   - 넉넉한 여백 (24px-48px)
   - 세련된 타이포그래피 계층
```

---

## 2. 컬러 시스템

### 2.1 Primary: 번개장터 시그니처

| 토큰 | 값 | 용도 | 사용 원칙 |
|------|-----|------|---------|
| `--color-primary` | `#FF6B54` | CTA, 활성 상태 | **절제적 사용** - 화면당 1-2곳 |
| `--color-primary-hover` | `#FF5640` | 호버 상태 | Primary 요소 인터랙션 |
| `--color-primary-light` | `#FFF0EE` | 배경 강조 | Subtle한 배경 처리 |
| `--color-primary-dark` | `#E65545` | 눌림 상태 | Active 상태 피드백 |

### 2.2 Mono: 텍스트 계층 (핵심)

```css
/* 5단계 모노톤 팔레트 - 명확한 계층 구조 */
--color-text-primary: #0F0F0F;      /* 제목, 핵심 메시지 */
--color-text-secondary: #3D3D3D;    /* 본문 텍스트 */
--color-text-tertiary: #6B6B6B;     /* 보조 설명 */
--color-text-muted: #9A9A9A;        /* 메타 정보 */
--color-text-disabled: #CBCBCB;     /* 비활성 상태 */
```

**명도 대비율 기준**:
- Primary → Background: 14.8:1 (WCAG AAA)
- Secondary → Background: 10.2:1 (AAA)
- Tertiary → Background: 5.9:1 (AA)

### 2.3 Background: 클린 & 레이어링

| 토큰 | 값 | 용도 |
|------|-----|------|
| `--color-bg` | `#FFFFFF` | 메인 배경 (순백) |
| `--color-surface` | `#FAFAFA` | 카드, 섹션 배경 |
| `--color-surface-elevated` | `#F5F5F5` | 호버 상태 |
| `--color-surface-subtle` | `#EEEEEE` | 구분선, 비활성 영역 |

### 2.4 Border & Shadow: 미세한 구분

```css
/* 세련된 경계선 */
--color-border: #E0E0E0;            /* 기본 보더 */
--color-border-light: #ECECEC;      /* 서브틀 구분 */
--color-border-dark: #C4C4C4;       /* 강조 보더 */

/* 프리미엄 그림자 (alpha 낮춤) */
--shadow-xs: 0 1px 2px rgba(0,0,0,0.04);
--shadow-sm: 0 2px 4px rgba(0,0,0,0.06);
--shadow-md: 0 4px 12px rgba(0,0,0,0.08);
--shadow-lg: 0 8px 24px rgba(0,0,0,0.1);
--shadow-xl: 0 16px 48px rgba(0,0,0,0.12);
```

### 2.5 Semantic: 최소한의 시스템 컬러

| 상태 | 컬러 | 용도 | 사용 빈도 |
|------|------|------|---------|
| Success | `#22C55E` | 완료, 성공 메시지 | 극소 |
| Warning | `#F59E0B` | 주의 안내 | 극소 |
| Danger | `#EF4444` | 에러, 경고 | 극소 |
| Info | `#3B82F6` | 정보 표시 | 거의 안씀 |

> **원칙**: Semantic 컬러는 실제 상태 표시에만 사용. 장식 목적으로 사용 금지.

---

## 3. 타이포그래피 시스템

### 3.1 Font Stack

```css
--font-family: 'Pretendard', -apple-system, BlinkMacSystemFont,
               'Apple SD Gothic Neo', 'Noto Sans KR', sans-serif;
--font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;
```

**선택 이유**:
- **Pretendard**: 한글 가독성 + 모던한 느낌, 번개장터와 어울리는 중립적 폰트
- 시스템 폰트 우선순위로 빠른 로딩

### 3.2 크기 & 계층

| 레벨 | 토큰 | 크기 | 줄높이 | 굵기 | 용도 |
|------|------|------|--------|------|------|
| **H1** | `--font-size-5xl` | `56px` | `1.2` | `700` | 섹션 타이틀 |
| **H2** | `--font-size-4xl` | `44px` | `1.25` | `700` | 슬라이드 제목 |
| **H3** | `--font-size-3xl` | `32px` | `1.3` | `600` | 소제목 |
| **H4** | `--font-size-2xl` | `24px` | `1.4` | `600` | 카드 제목 |
| **Body-Large** | `--font-size-lg` | `18px` | `1.7` | `400` | 강조 본문 |
| **Body** | `--font-size-base` | `16px` | `1.75` | `400` | 일반 본문 |
| **Body-Small** | `--font-size-sm` | `14px` | `1.6` | `400` | 보조 설명 |
| **Caption** | `--font-size-xs` | `12px` | `1.5` | `500` | 메타 정보 |

### 3.3 굵기(Weight) 사용 원칙

```
700 (Bold) - 섹션 타이틀, H1-H2만
600 (SemiBold) - H3-H4, 강조 버튼 레이블
500 (Medium) - 테이블 헤더, 작은 레이블
400 (Regular) - 모든 본문
```

### 3.4 발표 환경 최적화

**프로젝터 가시성 기준**:
- 최소 크기: 20px
- 권장 본문: 24px 이상
- 제목: 44px 이상
- 줄 간격: 최소 1.6 (장문은 1.75)

---

## 4. Spacing & Layout

### 4.1 간격 토큰 (4px 배수 시스템)

```css
--space-2xs: 2px;    /* 미세 조정 */
--space-xs: 4px;     /* 텍스트-아이콘 간격 */
--space-sm: 8px;     /* 밀접한 요소 */
--space-md: 12px;    /* 기본 내부 여백 */
--space-base: 16px;  /* 표준 간격 */
--space-lg: 24px;    /* 카드 패딩 */
--space-xl: 32px;    /* 섹션 간격 */
--space-2xl: 48px;   /* 큰 여백 */
--space-3xl: 64px;   /* 슬라이드 구분 */
--space-4xl: 96px;   /* 최대 여백 */
```

### 4.2 레이아웃 그리드

```
- 사이드바: 360px 고정
- 메인 콘텐츠: 최대 1200px (중앙 정렬)
- 카드 그리드: 2열 (gap: 24px)
- 테이블 그리드: 3-4열 (gap: 16px)
```

### 4.3 안전 영역 (발표 환경)

```
- 상단/하단: 64px
- 좌우: 48px (사이드바 제외)
- 슬라이드 여백: 최소 48px
```

---

## 5. 컴포넌트 스타일 가이드

### 5.1 Cards (카드)

#### Tool Card (도구 카드)

```css
.tool-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: 12px;
  padding: 24px;
  box-shadow: var(--shadow-sm);
  transition: all 0.2s ease;
}

.tool-card:hover {
  border-color: var(--color-primary);
  box-shadow: var(--shadow-md);
  transform: translateY(-2px);
}

.tool-card h4 {
  font-size: 20px;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 8px;
}

.tool-card p {
  font-size: 15px;
  color: var(--color-text-tertiary);
  line-height: 1.6;
}
```

**Before (Purple Theme)**:
- Primary color: #6C5CE7 everywhere
- Heavy shadows
- Bold borders

**After (번개장터 Theme)**:
- Monotone default state
- Primary only on hover border
- Subtle shadows (0.06 alpha)

#### AI Card (AI 도구 카드)

```css
.ai-card {
  background: linear-gradient(135deg, #FAFAFA 0%, #F5F5F5 100%);
  border: 1px solid var(--color-border-light);
  border-radius: 16px;
  padding: 32px 24px;
  text-align: center;
  transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

.ai-card:hover {
  background: var(--color-primary-light);
  border-color: var(--color-primary);
  box-shadow: 0 8px 20px rgba(255, 107, 84, 0.15);
}

.ai-card-icon {
  width: 56px;
  height: 56px;
  margin: 0 auto 16px;
  background: var(--color-text-disabled);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 28px;
}

.ai-card:hover .ai-card-icon {
  background: var(--color-primary);
  color: white;
}
```

**컬러 전략**:
- Default: 회색 그라디언트 (비활성 느낌)
- Hover: 번개장터 코랄 활성화 (강한 인상)

#### Case Card (실제 사례 카드)

```css
.case-card {
  background: var(--color-bg);
  border: 2px solid var(--color-border);
  border-radius: 16px;
  padding: 28px;
  cursor: pointer;
  position: relative;
  overflow: hidden;
}

.case-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 4px;
  height: 100%;
  background: var(--color-text-disabled);
  transition: background 0.3s ease;
}

.case-card:hover::before {
  background: var(--color-primary);
}

.case-card-badge {
  display: inline-block;
  padding: 4px 12px;
  background: var(--color-surface-elevated);
  color: var(--color-text-tertiary);
  border-radius: 6px;
  font-size: 13px;
  font-weight: 500;
  margin-bottom: 12px;
}

.case-card:hover .case-card-badge {
  background: var(--color-primary);
  color: white;
}

.case-card h4 {
  font-size: 22px;
  font-weight: 600;
  color: var(--color-text-primary);
  margin-bottom: 12px;
}

.case-card p {
  font-size: 15px;
  color: var(--color-text-tertiary);
  line-height: 1.65;
}
```

**인터랙션 원칙**:
- 좌측 보더 + 뱃지가 동시에 번개장터 컬러로 전환
- 미세한 애니메이션 (0.3s cubic-bezier)

### 5.2 Tables (표)

```css
table {
  width: 100%;
  border-collapse: separate;
  border-spacing: 0;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: 12px;
  overflow: hidden;
}

thead {
  background: var(--color-surface-elevated);
}

thead th {
  padding: 16px 20px;
  text-align: left;
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text-secondary);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  border-bottom: 2px solid var(--color-border);
}

tbody td {
  padding: 16px 20px;
  font-size: 15px;
  color: var(--color-text-secondary);
  border-bottom: 1px solid var(--color-border-light);
}

tbody tr:last-child td {
  border-bottom: none;
}

tbody tr:hover {
  background: var(--color-surface-subtle);
}

/* 강조 셀 */
td.highlight {
  font-weight: 600;
  color: var(--color-text-primary);
}

td.accent {
  color: var(--color-primary);
  font-weight: 600;
}
```

**Before → After**:
- Purple header bg → 밝은 회색
- Heavy row borders → 미세한 구분선
- 강조는 `color: primary` 로만 처리

### 5.3 Code Blocks (코드 블록)

```css
pre {
  background: #1E1E1E;  /* VS Code Dark */
  border: 1px solid #2D2D2D;
  border-radius: 8px;
  padding: 20px 24px;
  overflow-x: auto;
  font-family: var(--font-mono);
  font-size: 14px;
  line-height: 1.6;
  color: #D4D4D4;
  box-shadow: var(--shadow-sm);
}

code {
  font-family: var(--font-mono);
  font-size: 0.9em;
  background: var(--color-surface-elevated);
  padding: 2px 6px;
  border-radius: 4px;
  color: var(--color-primary);
}

/* 인라인 코드는 번개장터 컬러 */
p code, li code {
  background: var(--color-primary-light);
  color: var(--color-primary-dark);
  font-weight: 500;
}
```

**컬러 사용**:
- Block: 다크 배경 (대비 극대화)
- Inline: 번개장터 컬러로 눈에 띄게

### 5.4 Blockquotes (인용문)

```css
blockquote {
  position: relative;
  padding: 20px 24px 20px 28px;
  margin: 24px 0;
  background: var(--color-surface);
  border-left: 4px solid var(--color-primary);
  border-radius: 0 8px 8px 0;
  font-style: italic;
  color: var(--color-text-secondary);
  box-shadow: var(--shadow-xs);
}

blockquote::before {
  content: '"';
  position: absolute;
  top: 16px;
  left: 12px;
  font-size: 32px;
  color: var(--color-primary);
  opacity: 0.3;
  font-family: Georgia, serif;
}

blockquote p {
  margin: 0;
  font-size: 17px;
  line-height: 1.7;
}

blockquote footer {
  margin-top: 12px;
  font-size: 14px;
  font-style: normal;
  color: var(--color-text-muted);
}

blockquote footer::before {
  content: '— ';
}
```

**포인트**:
- 좌측 보더만 번개장터 코랄 사용
- Quote mark도 동일 컬러 (투명도 30%)

### 5.5 Insight Boxes (인사이트 박스)

```css
.insight-box {
  background: linear-gradient(135deg, #FFF0EE 0%, #FFE8E5 100%);
  border: 1px solid var(--color-primary);
  border-radius: 12px;
  padding: 24px 28px;
  margin: 32px 0;
  position: relative;
}

.insight-box::before {
  content: '💡';
  position: absolute;
  top: -16px;
  left: 24px;
  font-size: 32px;
  background: var(--color-bg);
  padding: 0 8px;
}

.insight-box h4 {
  font-size: 18px;
  font-weight: 600;
  color: var(--color-primary);
  margin-bottom: 8px;
}

.insight-box p {
  font-size: 16px;
  color: var(--color-text-secondary);
  line-height: 1.7;
  margin: 0;
}
```

**Before → After**:
- Purple gradient → 번개장터 코랄 gradient (light)
- 제목 컬러도 primary

### 5.6 Key Message Boxes (핵심 메시지 박스)

```css
.key-message {
  background: var(--color-text-primary);
  color: var(--color-bg);
  padding: 40px 48px;
  border-radius: 16px;
  text-align: center;
  margin: 48px 0;
  box-shadow: var(--shadow-lg);
  position: relative;
  overflow: hidden;
}

.key-message::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 4px;
  background: var(--color-primary);
}

.key-message h3 {
  font-size: 32px;
  font-weight: 700;
  margin-bottom: 16px;
  color: white;
}

.key-message p {
  font-size: 18px;
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.85);
  margin: 0;
}
```

**전략**:
- 배경: 검은색 (강한 대비)
- 하단 보더만 번개장터 코랄 (액센트)

### 5.7 Buttons (버튼)

```css
.btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 12px 24px;
  font-size: 16px;
  font-weight: 600;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.2s ease;
  border: none;
  text-decoration: none;
}

/* Primary Button - 번개장터 코랄 */
.btn-primary {
  background: var(--color-primary);
  color: white;
  box-shadow: 0 4px 12px rgba(255, 107, 84, 0.25);
}

.btn-primary:hover {
  background: var(--color-primary-hover);
  box-shadow: 0 6px 20px rgba(255, 107, 84, 0.35);
  transform: translateY(-2px);
}

.btn-primary:active {
  background: var(--color-primary-dark);
  transform: translateY(0);
}

/* Secondary Button - 모노톤 */
.btn-secondary {
  background: var(--color-surface-elevated);
  color: var(--color-text-primary);
  border: 1px solid var(--color-border);
}

.btn-secondary:hover {
  background: var(--color-surface-subtle);
  border-color: var(--color-text-tertiary);
}

/* Ghost Button - 투명 */
.btn-ghost {
  background: transparent;
  color: var(--color-text-secondary);
  border: 1px solid var(--color-border);
}

.btn-ghost:hover {
  background: var(--color-surface);
  color: var(--color-primary);
  border-color: var(--color-primary);
}
```

**버튼 위계**:
- Primary (코랄) - 주요 CTA 1-2개만
- Secondary (회색) - 일반 액션
- Ghost (투명) - 서브 액션

### 5.8 Badges (뱃지)

```css
.badge {
  display: inline-block;
  padding: 4px 12px;
  font-size: 13px;
  font-weight: 500;
  border-radius: 6px;
  white-space: nowrap;
}

.badge-default {
  background: var(--color-surface-elevated);
  color: var(--color-text-tertiary);
}

.badge-primary {
  background: var(--color-primary-light);
  color: var(--color-primary);
}

.badge-success {
  background: #DCFCE7;
  color: #16A34A;
}

.badge-outline {
  background: transparent;
  border: 1px solid var(--color-border);
  color: var(--color-text-tertiary);
}
```

### 5.9 Navigation (사이드바)

```css
.sidebar {
  width: 360px;
  background: var(--color-bg);
  border-right: 1px solid var(--color-border);
  padding: 32px 0;
  position: fixed;
  height: 100vh;
  overflow-y: auto;
}

.sidebar-logo {
  padding: 0 24px 24px;
  border-bottom: 1px solid var(--color-border-light);
  margin-bottom: 24px;
}

.sidebar-logo h1 {
  font-size: 24px;
  font-weight: 700;
  color: var(--color-text-primary);
}

.sidebar-nav li {
  list-style: none;
}

.sidebar-nav a {
  display: block;
  padding: 12px 24px;
  font-size: 15px;
  font-weight: 500;
  color: var(--color-text-tertiary);
  text-decoration: none;
  border-left: 3px solid transparent;
  transition: all 0.2s ease;
}

.sidebar-nav a:hover {
  background: var(--color-surface);
  color: var(--color-text-primary);
}

.sidebar-nav a.active {
  background: var(--color-primary-light);
  color: var(--color-primary);
  border-left-color: var(--color-primary);
  font-weight: 600;
}
```

**Before → After**:
- Active state: Purple → 번개장터 코랄
- Hover: 미세한 배경 변화만
- 좌측 보더로 활성 상태 명확화

---

## 6. CSS 변수 매핑 (Before → After)

### 기존 (Purple Theme) → 새로운 (번개장터 Theme)

| 변수 | Before | After | 변경 사유 |
|------|--------|-------|---------|
| `--color-primary` | `#6C5CE7` | `#FF6B54` | 번개장터 시그니처 코랄 |
| `--color-primary-light` | `#A29BFE` | `#FFF0EE` | 밝은 배경용 코랄 tint |
| `--color-primary-dark` | `#5A4BD1` | `#E65545` | Active state용 어두운 코랄 |
| `--color-bg` | `#FAFBFC` | `#FFFFFF` | 순백 배경 (더 깔끔) |
| `--color-surface` | `#FFFFFF` | `#FAFAFA` | 카드 배경 (미세한 회색) |
| `--color-surface-elevated` | `#FFFFFF` | `#F5F5F5` | 호버 상태 |
| `--color-surface-subtle` | `#F1F3F5` | `#EEEEEE` | 비활성 영역 |
| `--color-text` | `#1A1A2E` | `#0F0F0F` | 더 진한 검정 (대비 증가) |
| `--color-text-secondary` | `#4A5568` | `#3D3D3D` | 본문용 다크 그레이 |
| `--color-text-muted` | `#718096` | `#9A9A9A` | 메타 정보 (더 밝은 회색) |
| `--color-border` | `#E2E8F0` | `#E0E0E0` | 중립적 회색 보더 |
| `--color-border-light` | `#EDF2F7` | `#ECECEC` | 서브틀 구분선 |

### 그림자 투명도 조정

```css
/* Before - 너무 진함 */
--shadow-md: 0 4px 12px rgba(0,0,0,0.08);

/* After - 더 세련됨 */
--shadow-md: 0 4px 12px rgba(0,0,0,0.06);
```

---

## 7. 적용 원칙

### 7.1 색상 적용 우선순위

```
1순위: 모노톤 (회색 5단계)
2순위: 번개장터 코랄 (CTA, Active, Accent만)
3순위: Semantic (실제 상태 표시만)
```

### 7.2 컬러 사용 체크리스트

- [ ] 화면당 번개장터 코랄은 1-2개 요소만
- [ ] 모든 텍스트는 회색 팔레트 5단계 중 하나
- [ ] 배경은 순백 or 밝은 회색만
- [ ] 호버 상태도 가능하면 모노톤 (보더만 코랄)
- [ ] Semantic 컬러는 실제 상태에만 사용

### 7.3 프리미엄 느낌 체크리스트

- [ ] 카드 그림자: alpha 0.06-0.08 (미세하게)
- [ ] 보더: 1px 두께 (얇게)
- [ ] 패딩: 24px-32px (넉넉하게)
- [ ] 텍스트 계층: 5단계 명확히 구분
- [ ] 인터랙션: 0.2-0.3s cubic-bezier

### 7.4 접근성 검증

- [ ] 텍스트-배경 대비: 최소 4.5:1 (본문), 3:1 (큰 글씨)
- [ ] 번개장터 코랄 (#FF6B54) - 흰 배경에서 3.5:1 (AA Large 통과)
- [ ] 포커스 표시: 3px outline, 번개장터 코랄
- [ ] 버튼 터치 영역: 최소 44px × 44px

---

## 8. Before/After 미리보기

### 8.1 Tool Card 변화

#### Before (Purple Theme)
```
┌─────────────────────────────┐
│ 🎯 Claude Code             │  ← #6C5CE7 everywhere
│                             │
│ AI 코딩 어시스턴트...       │  ← Purple text
│                             │
└─────────────────────────────┘
  ↓ Heavy shadow, purple border
```

#### After (번개장터 Theme)
```
┌─────────────────────────────┐
│ 🎯 Claude Code             │  ← 회색 (#3D3D3D)
│                             │
│ AI 코딩 어시스턴트...       │  ← 밝은 회색 (#6B6B6B)
│                             │
└─────────────────────────────┘
  ↓ Subtle shadow (alpha 0.06), light gray border

  [Hover]
┌─────────────────────────────┐
│ 🎯 Claude Code             │  ← Border → #FF6B54
│                             │
│ AI 코딩 어시스턴트...       │
│                             │
└─────────────────────────────┘
```

### 8.2 Button 변화

#### Before
```
┌─────────────┐  ┌─────────────┐
│ 시작하기     │  │ 더보기       │
└─────────────┘  └─────────────┘
 #6C5CE7          #A29BFE
 (Purple)         (Light Purple)
```

#### After
```
┌─────────────┐  ┌─────────────┐
│ 시작하기     │  │ 더보기       │
└─────────────┘  └─────────────┘
 #FF6B54          #F5F5F5
 (코랄 - CTA)      (회색 - 일반)
```

### 8.3 Sidebar Navigation 변화

#### Before
```
├─ 오버뷰              ← Inactive (회색)
├─ 바이브 코딩이란?     ← Active (#6C5CE7)
├─ 마인드셋 전환        ← Inactive
```

#### After
```
├─ 오버뷰              ← Inactive (#6B6B6B)
├─ 바이브 코딩이란?     ← Active (#FF6B54) + 좌측 보더
├─ 마인드셋 전환        ← Inactive
```

### 8.4 Case Card 변화

#### Before
```
┌──────────────────────────────┐
│ [STARTUP]                    │ ← Purple badge
│                               │
│ Photo AI                     │ ← Purple title
│                               │
│ AI 헤드샷 생성 서비스...      │
└──────────────────────────────┘
```

#### After
```
┌──────────────────────────────┐
│ [STARTUP]                    │ ← 회색 badge
│                               │
│ Photo AI                     │ ← 검정 title
│                               │
│ AI 헤드샷 생성 서비스...      │ ← 회색 본문
└──────────────────────────────┘
   ↑ 좌측 보더 (회색)

[Hover]
┌──────────────────────────────┐
│ [STARTUP]                    │ ← #FF6B54 badge
│                               │
│ Photo AI                     │
│                               │
│ AI 헤드샷 생성 서비스...      │
└──────────────────────────────┘
   ↑ 좌측 보더 → #FF6B54
```

---

## 9. 구현 가이드

### 9.1 단계별 적용 순서

```
1. CSS 변수 교체 (styles.css :root 섹션)
2. 컴포넌트별 클래스 재작성
   - Cards → Buttons → Tables → Badges
3. 호버/활성 상태 재정의
4. 접근성 검증 (대비율 체크)
5. 실제 프로젝터 테스트 (5m 거리)
```

### 9.2 빠른 교체 스크립트 (CSS 변수만)

```css
:root {
  /* Primary: Purple → 번개장터 코랄 */
  --color-primary: #FF6B54;
  --color-primary-hover: #FF5640;
  --color-primary-light: #FFF0EE;
  --color-primary-dark: #E65545;

  /* Background: 순백 기반 */
  --color-bg: #FFFFFF;
  --color-surface: #FAFAFA;
  --color-surface-elevated: #F5F5F5;
  --color-surface-subtle: #EEEEEE;

  /* Text: 5단계 모노톤 */
  --color-text-primary: #0F0F0F;
  --color-text-secondary: #3D3D3D;
  --color-text-tertiary: #6B6B6B;
  --color-text-muted: #9A9A9A;
  --color-text-disabled: #CBCBCB;

  /* Border & Shadow */
  --color-border: #E0E0E0;
  --color-border-light: #ECECEC;
  --color-border-dark: #C4C4C4;

  --shadow-xs: 0 1px 2px rgba(0,0,0,0.04);
  --shadow-sm: 0 2px 4px rgba(0,0,0,0.06);
  --shadow-md: 0 4px 12px rgba(0,0,0,0.08);
  --shadow-lg: 0 8px 24px rgba(0,0,0,0.1);
  --shadow-xl: 0 16px 48px rgba(0,0,0,0.12);
}
```

### 9.3 주의사항

```
⚠️ 하드코딩된 색상 찾기
- 인라인 스타일에서 rgba(108, 92, 231, ...) 제거
- #6C5CE7 직접 참조 모두 var(--color-primary)로 변경

⚠️ 모달/팝업 배경
- 오버레이: rgba(0,0,0,0.6) → rgba(0,0,0,0.5) (더 투명)

⚠️ 프로젝터 테스트 필수
- 실제 발표 환경에서 가독성 검증
- 최소 3m 거리에서 읽히는지 확인
```

---

## 10. 유지보수 가이드

### 10.1 새 컴포넌트 추가 시 원칙

```
1. Default state는 모노톤 (회색 팔레트)
2. Interactive state (hover/active)에만 번개장터 코랄
3. 그림자는 alpha 0.06-0.1 범위
4. 패딩은 --space-* 토큰 사용 (4px 배수)
```

### 10.2 A/B 테스트 기준

| 요소 | 기준 | 측정 방법 |
|------|------|---------|
| 가독성 | 5m 거리에서 읽힘 | 실제 프로젝터 테스트 |
| 대비율 | 4.5:1 이상 | WebAIM Contrast Checker |
| 클릭률 | Primary CTA 15% 이상 | 사용자 테스트 |
| 브랜드 인지 | 번개장터 연상도 | 설문 조사 |

### 10.3 다크모드 변환 (미래)

현재는 라이트 모드만 지원하지만, 향후 다크모드 추가 시:

```css
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg: #0F0F0F;
    --color-surface: #1A1A1A;
    --color-text-primary: #FFFFFF;
    --color-primary: #FF8570; /* 코랄을 더 밝게 */

    /* 그림자 대신 보더 강조 */
    --shadow-md: none;
    --color-border: #2D2D2D;
  }
}
```

---

## 마무리: 핵심 원칙 요약

```
✅ 모노톤 위주 - 전체 레이아웃은 흑백 스펙트럼
✅ 절제된 컬러 - 번개장터 코랄은 CTA + Active에만
✅ 프리미엄 디테일 - 미세한 그림자, 얇은 보더, 넉넉한 여백
✅ 명확한 계층 - 텍스트 5단계, 컬러 3단계
✅ 가독성 최우선 - 대비율 4.5:1 이상, 프로젝터 테스트 필수
```

**브랜드 메시지**: *"번개처럼 빠르고 명확하게 - 번개장터 x 바이브 코딩"*

---

**문서 버전**: 1.0
**작성일**: 2026-02-10
**작성자**: UXUI 전문가 에이전트
**적용 대상**: 바이브 코딩 특강 프레젠테이션 (HTML/CSS)
